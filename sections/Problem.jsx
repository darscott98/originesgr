export default function Problem() {
  return (
    <section className="section" aria-labelledby="problem-title">
      <div className="container narrow">
        <h2 id="problem-title">Most teams lose momentum between planning and execution.</h2>
        <p>
          Requests live in chats, ownership is unclear, and updates are scattered. Work slips because nobody has a single, trusted view of what matters now.
        </p>
      </div>
    </section>
  );
}
